<% div_for comment do %>
  <div id = "comment_author">
  	<%= comment.name %>
  	<% if can? :edit, comment %>
  	  <%= link_to 'Edit', edit_article_comment_path(@article, comment) %>
  	<% end %>
  	<% if can? :destroy, comment %>
  	  <%= link_to 'Delete', article_comment_path(@article, comment), :confirm => 'Are you sure?', :method => :delete %>
  	<% end %>
  </div
  <div id = "comment_created_at">
  	<%= comment.created_at %>
  </div>
  <div id = "comment_text">
	<p><%= comment.body %></p>
  </div>
<% end %>