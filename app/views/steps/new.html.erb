<h1>New step for: <%= @tutorial.name %></h1>

<%= form_for([@tutorial, @step]) do |f| %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <p><%= f.submit %></p>
<% end %>

<%= link_to 'Back', @tutorial %>